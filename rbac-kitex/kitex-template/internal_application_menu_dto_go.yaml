# ncgo exported template — internal/application/menu/dto.go
path: internal/application/menu/dto.go
update_behavior:
    type: skip
body: |
    package menusvc

    // CreateMenuInput carries a menu creation request.
    type CreateMenuInput struct {{ "{" }}
    	ParentID  int64
    	Type      string
    	Name      string
    	Path      string
    	Component string
    	PermCode  string
    	SortOrder int32
    {{ "}" }}

    // UpdateMenuInput carries a partial menu update; nil fields are left unchanged.
    type UpdateMenuInput struct {{ "{" }}
    	ID        int64
    	ParentID  *int64
    	Type      *string
    	Name      *string
    	Path      *string
    	Component *string
    	PermCode  *string
    	SortOrder *int32
    {{ "}" }}
