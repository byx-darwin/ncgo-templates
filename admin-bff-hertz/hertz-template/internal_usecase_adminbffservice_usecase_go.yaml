# ncgo exported template — internal/usecase/adminbffservice/usecase.go
path: internal/usecase/adminbffservice/usecase.go
update_behavior:
    type: skip
loop_service: true
body: |-
    package adminbffservice

    // adminServiceRepo defines the data-access port for this usecase.
    // Implement it in internal/repository/adminbffservice/.
    type adminServiceRepo interface {{ "{" }}
    	// TODO: declare repository methods needed by the usecase
    {{ "}" }}

    // UseCase orchestrates business logic for adminService.
    // It depends on interfaces (ports), not concrete implementations.
    type UseCase struct {{ "{" }}
    	repo adminServiceRepo
    {{ "}" }}

    // New creates a UseCase wired to the given repository.
    func New(repo adminServiceRepo) *UseCase {{ "{" }}
    	return &UseCase{{ "{" }}repo: repo{{ "}" }}
    {{ "}" }}

    // Add HTTP methods to the IDL proto, then rerun `make update`.
    // Implement handler methods here as:
    //   func (uc *UseCase) Ping(ctx context.Context, req *pb.PingReq) (*pb.PingResp, error) {{ "{" }}
    //       // Raw literal 10010 collides with frameworkerror.CodeRPCUnavailable; use
    //       // response.CodeNotImplemented (10402) instead, or a domain-specific error code.
    //       return nil, goerror.In("adminbffservice.usecase").Code(response.CodeNotImplemented).Public("not_implemented").Errorf("Ping: not implemented")
    //   {{ "}" }}
