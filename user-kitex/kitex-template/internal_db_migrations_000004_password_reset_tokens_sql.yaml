# Goose migration for internal/db/migrations/000004_password_reset_tokens.sql
path: internal/db/migrations/000004_password_reset_tokens.sql
update_behavior:
  type: cover
body: |-
    -- +goose Up
    CREATE TABLE password_reset_tokens (
        id UUID PRIMARY KEY,
        user_id UUID NOT NULL REFERENCES users(id) ON DELETE CASCADE,
        channel TEXT NOT NULL,          -- 'email' | 'sms'
        credential_hash TEXT NOT NULL,  -- sha256 hex of the token/code, never the raw value
        expires_at TIMESTAMPTZ NOT NULL,
        used_at TIMESTAMPTZ,
        created_at TIMESTAMPTZ NOT NULL DEFAULT now()
    );

    CREATE INDEX idx_password_reset_tokens_user_id ON password_reset_tokens(user_id);
    CREATE UNIQUE INDEX idx_password_reset_tokens_credential_hash ON password_reset_tokens(credential_hash);

    -- +goose Down
    DROP TABLE IF EXISTS password_reset_tokens;
