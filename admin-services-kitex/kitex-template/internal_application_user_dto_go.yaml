# ncgo exported template — internal/application/user/dto.go
path: internal/application/user/dto.go
update_behavior:
    type: skip
body: |
    package usersvc

    import "{{.Module}}/internal/domain/user"

    // CreateUserInput is the command to create a user.
    type CreateUserInput struct {{ "{" }}
    	Username string
    	Password string
    	Nickname string
    	Avatar   string
    	Email    string
    	Phone    string
    {{ "}" }}

    // UpdateUserInput is the command to update a user (all fields optional).
    type UpdateUserInput struct {{ "{" }}
    	ID       int64
    	Password *string
    	Status   *int
    	Nickname *string
    	Avatar   *string
    	Email    *string
    	Phone    *string
    {{ "}" }}

    // ListUsersResult is a page of users plus the total count.
    type ListUsersResult struct {{ "{" }}
    	Users []*user.User
    	Total int32
    {{ "}" }}
