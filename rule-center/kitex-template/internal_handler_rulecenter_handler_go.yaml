# ncgo exported template — internal/handler/rulecenter/handler.go
path: internal/handler/rulecenter/handler.go
update_behavior:
    type: skip
loop_service: true
body: |
    // Code generated by ncgo rule-center preset.

    package rulecenterhandler

    import (
    	"context"

    	"{{.Module}}/internal/pkg/rpcerror"
    	usecase "{{.Module}}/internal/usecase/rulecenter"
    	ratelimitv1 "{{.Module}}/kitex_gen/api/ratelimit/v1"
    )

    // {{.ServiceName}}ServiceImpl implements the Kitex-generated {{.ServiceName}}Service server interface.
    type {{.ServiceName}}ServiceImpl struct {{ "{" }}
    	uc *usecase.UseCase
    {{ "}" }}

    func New{{.ServiceName}}ServiceImpl(uc *usecase.UseCase) *{{.ServiceName}}ServiceImpl {{ "{" }}
    	return &{{.ServiceName}}ServiceImpl{{ "{" }}uc: uc{{ "}" }}
    {{ "}" }}

    func (s *{{.ServiceName}}ServiceImpl) Get{{.ServiceName}}(ctx context.Context, req *ratelimitv1.Get{{.ServiceName}}Req) (resp *ratelimitv1.Get{{.ServiceName}}Resp, err error) {{ "{" }}
    	{{ToLower .ServiceName}}, found, err := s.uc.Get{{.ServiceName}}(ctx, req.Service, req.Phase, req.Method, req.Path, req.AppKey)
    	if err != nil {{ "{" }}
    		return nil, rpcerror.ToBizError(err)
    	{{ "}" }}
    	return &ratelimitv1.Get{{.ServiceName}}Resp{{ "{" }}Found: found, {{.ServiceName}}: rule{{ "}" }}, nil
    {{ "}" }}

    func (s *{{.ServiceName}}ServiceImpl) Create{{.ServiceName}}(ctx context.Context, req *ratelimitv1.Create{{.ServiceName}}Req) (resp *ratelimitv1.Create{{.ServiceName}}Resp, err error) {{ "{" }}
    	id, err := s.uc.Create{{.ServiceName}}(ctx, req)
    	if err != nil {{ "{" }}
    		return nil, rpcerror.ToBizError(err)
    	{{ "}" }}
    	return &ratelimitv1.Create{{.ServiceName}}Resp{{ "{" }}Id: id{{ "}" }}, nil
    {{ "}" }}

    func (s *{{.ServiceName}}ServiceImpl) Update{{.ServiceName}}(ctx context.Context, req *ratelimitv1.Update{{.ServiceName}}Req) (resp *ratelimitv1.Update{{.ServiceName}}Resp, err error) {{ "{" }}
    	err = s.uc.Update{{.ServiceName}}(ctx, req)
    	if err != nil {{ "{" }}
    		return nil, rpcerror.ToBizError(err)
    	{{ "}" }}
    	return &ratelimitv1.Update{{.ServiceName}}Resp{{ "{" }}{{ "}" }}, nil
    {{ "}" }}

    func (s *{{.ServiceName}}ServiceImpl) Delete{{.ServiceName}}(ctx context.Context, req *ratelimitv1.Delete{{.ServiceName}}Req) (resp *ratelimitv1.Delete{{.ServiceName}}Resp, err error) {{ "{" }}
    	err = s.uc.Delete{{.ServiceName}}(ctx, req.Id)
    	if err != nil {{ "{" }}
    		return nil, rpcerror.ToBizError(err)
    	{{ "}" }}
    	return &ratelimitv1.Delete{{.ServiceName}}Resp{{ "{" }}{{ "}" }}, nil
    {{ "}" }}

    func (s *{{.ServiceName}}ServiceImpl) ListRules(ctx context.Context, req *ratelimitv1.ListRulesReq) (resp *ratelimitv1.ListRulesResp, err error) {{ "{" }}
    	page := req.Page
    	if page < 1 {{ "{" }}
    		page = 1
    	{{ "}" }}
    	pageSize := req.PageSize
    	if pageSize < 1 {{ "{" }}
    		pageSize = 20
    	{{ "}" }}
    	if pageSize > 100 {{ "{" }}
    		pageSize = 100
    	{{ "}" }}
    	rules, total, err := s.uc.ListRules(ctx, req.Service, req.Phase, page, pageSize)
    	if err != nil {{ "{" }}
    		return nil, rpcerror.ToBizError(err)
    	{{ "}" }}
    	return &ratelimitv1.ListRulesResp{{ "{" }}
    		Rules:    rules,
    		Total:    int32(total),
    		Page:     page,
    		PageSize: pageSize,
    	{{ "}" }}, nil
    {{ "}" }}
