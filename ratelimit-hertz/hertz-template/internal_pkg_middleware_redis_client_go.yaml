# Hertz custom template — internal/pkg/middleware/redis_client.go
# Generates the shared Redis client accessor used by middleware.
path: internal/pkg/middleware/redis_client.go
update_behavior:
    type: cover
body: |-
    package middleware

    import "github.com/redis/go-redis/v9"

    func sharedRedisClient(cfg any) redis.UniversalClient { return nil }
